{
  "name": "callbridge-telephony-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run --pool=threads --maxWorkers=1",
    "deploy": "wrangler deploy",
    "deploy:dry": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260813.1",
    "typescript": "^7.0.0",
    "vitest": "^4.1.10",
    "wrangler": "^4.122.0"
  }
}
